# Frontend Configuration
# Copy this file to .env and update the values

# Backend API URL
# NOTE: Must match FLASK_PORT in backend .env
# macOS users: use http://localhost:5001 (port 5000 is used by AirPlay Receiver)
REACT_APP_API_URL=http://localhost:5000
